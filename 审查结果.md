# Lumo 初步开发阶段审查结果

审查日期：2026-09-12

审查范围：Go 后端、React/TypeScript Console、认证与安全边界、数据库一致性、部署与发布流程、工程质量。

审查性质：只读审查。本报告不包含代码修改。

---

## 修复状态（2026-09-12 更新）

本报告的 26 项问题已逐项处置：**12 项 P1 + 12 项 P2 + 1 项 P3 全部修复**，1 项（正文信任模型）
按「净化 + 独立高危权限」的方案落地而非仅写文档。每项都补了可重复执行的回归测试：

- Go：`go test -tags nodynamic -count=1 ./...`（含集成测试，独立 `lumo_test` 库）全绿；`golangci-lint` 0 issues
- 前端：Vitest 66 通过（原 59 + 新增 7 条编辑器回归）；`tsc` 与 Biome 干净；生产构建通过
- 实机冒烟（临时库，验完已删）：登录失败第 9 次返回 429；Public 评论「有会话无 CSRF → 403、
  有 CSRF → 201、纯匿名 → 201、文章撤回后 → 404」全部符合预期

| # | 优先级 | 问题 | 状态 | 修复位置 | 回归测试 |
|---|---|---|---|---|---|
| 1 | P1 | 受限 PAT 可签发全权限 PAT | ✅ 已修复 | `internal/auth/{handler,parse,token,principal,service}.go`、`console/src/pages/profile.tsx` | `TestTokenIssuanceIsNotPrivilegeEscalation` |
| 2 | P1 | 正文脚本与 Console 同源执行 | ✅ 已修复 | `internal/content/sanitize.go`（新）、`handler.go`、`internal/app/permissions.go`、`internal/auth/perm/perm.go` | `TestSanitize`、`TestSanitizeForWriter`、`TestBodySanitizationIsPermissionGated`、`TestFrontendSanitizesUnprivilegedBody` |
| 3 | P1 | 登录缺限流与 Argon2 并发预算 | ✅ 已修复 | `internal/auth/ratelimit.go`（新）、`service.go`、`handler.go`、`internal/auth/password/password.go` | `TestLoginRateLimitEndToEnd`、`TestLoginLimiterWindow/Sweep/NilSafe`、`TestHashConcurrencyBudget` |
| 4 | P1 | 切换文章后仍显示上一篇却保存到新 ID | ✅ 已修复 | `console/src/pages/content/editor.tsx`（`kind + id` 作 key） | `editor.test.tsx`「按 id 重建编辑会话」 |
| 5 | P1 | 保存失败后仍继续发布旧稿 | ✅ 已修复 | `editor.tsx`（`saveAndPublish` + 写操作闸门） | 「保存失败时不再继续发布」「表单不脏时直接发布」 |
| 6 | P1 | 富文本切 Markdown 会清空正文 | ✅ 已修复 | `editor.tsx`（切换不动 `raw`，弹窗文案重写） | 「切换到 Markdown 不会清空正文」 |
| 7 | P1 | 分类/标签查询缓存结构冲突 | ✅ 已修复 | `console/src/lib/taxonomy.ts`（新）、`posts.tsx`、`editor.tsx` | 「分类控件用实体 ID 而不是 undefined」 |
| 8 | P1 | 站内导航绕过未保存提醒 | ✅ 已修复 | `console/src/main.tsx`、`app.tsx`、`components/navigation/unsaved-guard.tsx`（新）、`schema-form.tsx` | 「有未保存修改时，站内导航先弹确认」 |
| 9 | P1 | Loading 状态不能阻止重复提交 | ✅ 已修复 | `console/src/components/ui/button.tsx` + editor 的 `writeRef` | 「保存期间重复点击只会发出一个请求」 |
| 10 | P1 | 文章下线/转私密后评论仍公开读写 | ✅ 已修复 | `internal/comment/{model,store,handler}.go` | `TestCommentPublicVisibilityEndToEnd` |
| 11 | P1 | 永久删除与恢复并发删除已恢复内容 | ✅ 已修复 | `internal/content/store.go`（`DELETE ... AND status='trashed'`）、`handler.go` | `TestDeletePermanentlyIsAtomic` |
| 12 | P1 | 正式 Release 不受测试门禁约束 | ✅ 已修复 | `.github/workflows/verify.yml`（新）、`ci.yml`、`release.yml` | 需在有 Actions 的环境验证 |
| 1 | P2 | 单连接池导致迁移永久等待 | ✅ 已修复 | `internal/migrate/migrate.go`、`cmd/lumo/migrate.go`、`internal/config/config.go` | `TestSingleConnPoolMigrationDoesNotHang`、`TestSingleConnPoolWithDedicatedLockDB`、`TestLockWaitHonorsTimeout`、`TestLockWaitTimesOutWithClearError` |
| 2 | P2 | 合法三级菜单必被拒 | ✅ 已修复 | `internal/menu/handler.go` | `TestMenuEndToEnd`「校验：层级…」新增三级合法用例 |
| 3 | P2 | 评论最大长度设置不生效 | ✅ 已修复 | `internal/comment/handler.go`（`cleanContent` 带上限参数） | `TestCommentMaxLengthEndToEnd` |
| 4 | P2 | Public 评论写入缺 CSRF | ✅ 已修复 | `internal/api/api.go`、`internal/theme/builtin/ink/static/comments.js` | `TestCommentPublicWriteRequiresCSRF` |
| 5 | P2 | 并发保存菜单整树可能合并两份结果 | ✅ 已修复 | `internal/menu/store.go`（父行 `FOR UPDATE`） | `TestReplaceItemsIsSerialized`（已验证去掉锁会失败） |
| 6 | P2 | ZIP 目录条目绕过数量限制 | ✅ 已修复 | `internal/pkgzip/pkgzip.go`（`MaxEntries` 预算）、`internal/theme/package.go`、`internal/plugin/package.go` | `TestExtractEnforcesLimits` 两个新子测试 |
| 7 | P2 | 停机时邮件队列可能永久丢失 | ✅ 已修复 | `internal/mail/{service,module}.go`、`cmd/lumo/serve.go` | `TestSignalKeepsConsumerAliveUntilQueueDrained`、`TestShutdownReportsUndeliveredMail` |
| 8 | P2 | `/readyz` 缺少数据库探测期限 | ✅ 已修复 | `cmd/lumo/serve.go`（3 秒独立期限） | `TestReadyzTimesOutWith503` 等 4 条 |
| 9 | P2 | Compose 可能在优雅关闭前强杀应用 | ✅ 已修复 | `deploy/docker-compose.yml`（`stop_grace_period: 40s`，预算写在注释里） | 需在有 Docker 的环境演练 |
| 10 | P2 | 手动正式发布缺 GHCR 登录 | ✅ 已修复 | `.github/workflows/release.yml` | 需在有 Actions 的环境验证 |
| 11 | P2 | 特殊字符口令说明会造成首次部署失败 | ✅ 已修复 | `deploy/.env.example`、`deploy/docker-compose.yml`、`README.md` | 需按文档走一次首次部署 |
| 12 | P2 | 备份恢复说明不足 | ✅ 已修复 | `README.md`「备份与恢复」小节 | 需做一次备份恢复演练 |
| 13 | P3 | 构建不清理旧 Console 资源 | ✅ 已修复 | `Taskfile.yml`（`console:clean`）、`console/vite.config.ts` | `task console:build` 后 dist 只剩新产物 + `.gitkeep` |

### 与报告建议不同的决定

1. **P1-2 选了「服务端净化 + 独立高危权限」，没有把「所有编辑者都可信」只写进文档。**
   `content:unsafe_html` 默认只给 admin / super-admin；其余作者的 `content` 在保存时按允许列表净化，
   `raw` 原稿不净化。代价是 editor 不能嵌 iframe / 保留脚本钩子，编辑器里有明确提示；
   把该权限授予 editor 即可回到旧行为。
2. **P1-1 顺带反转了空 scopes 的语义**（继承全部权限 → 无权限，fail-closed）。
   按旧语义创建的令牌升级后失效，需重新签发。
3. **P1-6 没有实现真正的格式转换**（需要 turndown 一类库），改为「原稿原样交接、绝不清空 + 如实告知」。
   报告允许的「禁止非空文档切换」是备选方案，未采用。
4. **CI 触发方式未改回自动**：`verify.yml` 已可复用，`ci.yml` 的注释里写明加回 `push` / `pull_request`
   即可恢复推送自动执行——这是用户此前为省额度刻意关闭的，未擅自恢复。

### 仍需人工完成（本机条件限制）

- **容器停机演练**（`stop_grace_period` 是否够）与**备份恢复演练**：本机无 Docker
- **真实 Release 运行**与 CI 的 `-race` 作业：需在有 GitHub Actions 的环境跑一次
- **Playwright 端到端测试**：报告「主要测试缺口」里建议的浏览器级测试尚未引入，
  本次以组件级测试覆盖了编辑器的五类回归；移动端长表单、慢网重复提交等场景仍建议后续补上
- **本机 `-race`**：Windows 无 C 编译器，竞态检测只能依赖 CI 的测试作业

---

## 一、总体结论

项目基础质量较好，模块边界、OpenAPI 类型生成、数据库迁移测试、配置默认值、上传响应安全头等设计较扎实。

当前没有发现“未认证即可直接接管系统”的无条件 P0 漏洞，但存在多项发布前必须处理的高风险问题。若 Editor/Author 与管理员属于不同信任级别，正文脚本执行与受限 PAT 提权可升级为条件性 P0。

结论：

- 不建议在 P1 问题修复前推送正式版本标签或更新 `latest`。
- 建议先处理认证权限、同源脚本执行、编辑器数据丢失和评论可见性问题。
- 正式发布前必须补齐并发测试、端到端测试、容器停机演练和备份恢复演练。

## 二、验证结果

| 验证项 | 结果 |
|---|---|
| Go 全量测试 | 使用临时独立 PostgreSQL 17 实例运行，818 个测试/子测试通过，0 失败，0 跳过 |
| 前端测试 | 59 个 Vitest 测试通过 |
| 前端静态检查 | Biome、TypeScript 类型检查通过 |
| 前端生产构建 | 通过 |
| Go 静态检查 | `go vet`、`golangci-lint` 通过 |
| API 契约 | 在线 OpenAPI 与仓库生成文件的 paths、components 完全一致 |
| 浏览器验证 | 登录、仪表盘、编辑器、390px 移动布局正常，无控制台异常，无横向溢出 |
| 数据库隔离 | 使用临时独立 PostgreSQL 实例，未接触现有业务数据库 |
| 工作区状态 | 审查未修改工作区文件；临时测试服务、Chrome、PostgreSQL 均已清理 |

未覆盖或未实际执行的验证：

- Docker 容器停机实验。
- 真实 Release 流程。
- SMTP 停机与邮件排空实验。
- 特殊字符数据库密码的首次部署实验。
- 本机 `go test -race` 测试。

## 三、严重性定义

| 级别 | 含义 |
|---|---|
| P0 | 无条件、直接导致系统或管理员账号被接管，或造成不可控的数据破坏 |
| P1 | 发布前必须修复；涉及权限隔离、数据丢失、内容泄露、错误发布或核心功能失效 |
| P2 | 稳定版前应修复；涉及边缘配置、可靠性、资源耗尽或运维流程缺陷 |
| P3 | 优化项；当前没有已知功能故障，但会影响长期维护或交付质量 |

## 四、发布前必须处理的问题

### 1. 受限 PAT 可以签发账号全权限 PAT

✅ **已修复（2026-09-12）**：签发令牌只允许会话调用、必须重新验证密码，scope 必须是调用者权限子集；空 scopes 的语义反转为「无权限」（fail-closed），省略 scopes 时服务端展开成显式清单落库。`internal/auth/{handler,parse,token,principal,service}.go`，回归测试 `TestTokenIssuanceIsNotPrivilegeEscalation`。

优先级：P1

验证状态：已动态复现

位置：

- `internal/auth/handler.go:286-311`
- `internal/auth/principal.go:47-63`

问题：

仅包含 `posts:write` 的受限 PAT 可以调用令牌创建接口，并创建一个空 scopes 的新 PAT。空 scopes 在认证时解释为继承账号全部权限，而不是继承父 PAT 权限。

复现结果：

1. 使用 `posts:write` PAT 访问用户列表，返回 403。
2. 使用该 PAT 创建空 scopes 新令牌，返回 201。
3. 使用新令牌访问用户列表，返回 200。
4. 新令牌获得账号全部 22 项权限。

影响：

持有受限 PAT 的集成或自动化程序可以逃逸原有限制，获得账号完整权限，并可创建无过期时间、独立撤销的新令牌。

建议：

- 令牌创建仅允许会话认证，并要求重新验证密码。
- 若允许 PAT 派生，新令牌权限不得超过父 PAT。
- 限制派生令牌有效期，并建立父子撤销关系。
- 禁止使用空 scopes 表示账号无限权限。

### 2. 编辑器正文脚本与 Console 同源执行

✅ **已修复（2026-09-12）**：新增 `content:unsafe_html` 高危权限（默认仅 admin / super-admin，对应报告的方案 1 + 2）；无该权限的作者，渲染产物 `content` 在保存时按允许列表净化，`raw` 原稿不净化，恢复修订时按当前调用者权限重新净化。`internal/content/sanitize.go`（新）。测试：`TestSanitize`、`TestBodySanitizationIsPermissionGated`、`TestFrontendSanitizesUnprivilegedBody`。

优先级：P1；在 Editor/Author 与管理员属于不同信任级别时，可升级为条件性 P0

验证状态：静态确认

位置：

- `internal/content/render.go:22-35`
- `internal/theme/builtin/ink/templates/post.html:57-58`
- `internal/auth/session.go:225-233`

问题：

正文中的原始 HTML 与脚本不会被净化，并可由主题原样输出。浏览器会为同源管理请求自动携带 HttpOnly 会话 Cookie，脚本也可以读取非 HttpOnly 的 CSRF Cookie。

攻击条件：

- 攻击者拥有内容写入和发布权限，但没有用户或设置管理权限。
- 管理员随后访问攻击者发布的文章。

影响：

脚本可以在 CMS 同源执行并调用管理 API，破坏 Editor 与管理员之间的权限隔离。

项目说明：

`agent.md` 明确采用“信任已认证正文、不做净化”的策略。因此该问题同时是产品信任模型与角色隔离之间的冲突。

建议：

- 普通 Editor/Author 的正文使用服务端允许列表净化。
- 将“输出未经净化的 HTML”设为独立高危权限。
- 若必须允许任意脚本，应将 Console/管理 API 与公开正文隔离到不同源。
- 若所有内容编辑者都被视为站长同等可信，应在产品和安全文档中明确记录。

### 3. 登录接口缺少限流与 Argon2 并发预算

✅ **已修复（2026-09-12）**：失败尝试按账号（15 分钟 8 次）与客户端 IP（15 分钟 40 次）双维度计数，超限 429；argon2 加进程级并发闸门，额度用满返回 503 且不计入失败次数。`internal/auth/ratelimit.go`（新）、`internal/auth/password/password.go`。

优先级：P1

验证状态：静态确认

位置：

- `internal/auth/handler.go:34-43`
- `internal/auth/service.go:59-76`
- `internal/auth/password/password.go:126-132`
- `internal/server/router.go:80-96、107-114`

问题：

匿名登录请求即使账号不存在，也会执行一次约 64 MiB、3 轮 Argon2 校验。当前注册链未发现 IP/账号限流或进程级哈希并发上限。

影响：

- 小请求可以显著放大内存与 CPU 消耗。
- 缺少密码猜测防护。
- 前置反向代理若未额外配置限流，登录接口可被用于拒绝服务。

建议：

- 增加按账号和可信客户端 IP 的登录限流。
- 增加进程级有界哈希并发或内存预算。
- 超限返回 429。
- 不要只依赖前端按钮禁用或单一 IP 限流。

### 4. 切换文章后仍显示上一篇，却会保存到新 ID

✅ **已修复（2026-09-12）**：外层组件按 `kind + id` 加 key，切换内容即整体重建编辑会话（表单状态、`loaded`、编辑器实例一并重置）。`console/src/pages/content/editor.tsx`。

优先级：P1

验证状态：已动态复现

位置：

- `console/src/pages/content/editor.tsx:262-269`
- `console/src/pages/content/editor.tsx:375-377`

问题：

从文章 A 通过命令面板打开文章 B 时，组件不会因路由参数变化而重建，`loaded` 状态也不会随 ID 重置，因此 B 的数据到达后会被忽略。

复现结果：

- URL 已切换为 `/posts/3`。
- 标题和正文仍显示文章 A 的 `Review Alpha`。

影响：

用户可能在不知情的情况下把文章 A 的内容保存到文章 B，覆盖 B 的数据。

建议：

- 按 `kind + id` 隔离编辑会话。
- 切换内容时重新初始化所有表单状态和编辑器。
- 对未保存修改增加导航保护。

### 5. 保存失败后仍继续发布旧稿

✅ **已修复（2026-09-12）**：保存异常立即终止发布；新建内容改用保存响应中的 ID 发布（旧实现里新建后点发布其实什么都没发生）。发布与保存共用一个同步写操作闸门。

优先级：P1

验证状态：静态确认

位置：

- `console/src/pages/content/editor.tsx:674-680`

问题：

发布流程先执行保存，但通过 `.catch(() => {})` 吞掉保存异常，随后无条件下一步发布。

典型触发：

- 编辑已有草稿。
- 将 slug 改为另一篇已占用的值。
- 点击发布。

影响：

保存返回冲突后，用户仍可能看到发布操作继续，并错误发布服务器上的旧内容。

建议：

- 保存失败立即终止发布。
- 保存成功后使用保存响应中的 ID 执行发布。
- 将保存与发布纳入统一 pending 状态和完整错误处理。

### 6. 富文本切换 Markdown 会清空正文

✅ **已修复（2026-09-12）**：切换只改格式标记，**不再动 `raw`**（原稿原样交给另一个编辑器重新解析），确认弹窗文案改为如实说明「不清空、但不是无损」。

优先级：P1

验证状态：已动态复现

位置：

- `console/src/pages/content/editor.tsx:1297-1302`

问题：

切换格式时直接执行：

```ts
setRaw(pendingType === "markdown" ? "" : raw)
```

没有进行实际格式转换。已有正文在切换为 Markdown 时会被清空。

复现结果：

- 输入正文 `KEEP THIS REVIEW PARAGRAPH`。
- 选择 Markdown 并确认。
- 正文变空，原段落丢失。

建议：

- 实现 HTML/Markdown 转换，并保留切换前快照。
- 暂不支持转换时禁止非空文档切换。
- 若确实需要清空，应在确认框中明确提示整篇正文将被清除。

### 7. 分类和标签查询缓存结构冲突

✅ **已修复（2026-09-12）**：抽出 `useTaxonomyOptions`，缓存只存原始 DTO，列表页与编辑器各自转换；顺带修好列表页分类筛选实际传 `undefined` 的问题。`console/src/lib/taxonomy.ts`（新）。

优先级：P1

验证状态：已动态复现

位置：

- `console/src/pages/content/posts.tsx:400-414`
- `console/src/pages/content/editor.tsx:286-297`

问题：

文章列表和编辑器共用查询键 `["taxonomy-options"]`，但返回结构不同：

- 列表页面使用 `{ value, label }`。
- 编辑器期待 `{ id, name, ... }`。

复现结果：

打开文章列表后进入文章详情，分类控件 ID 变为：

```text
content-category-undefined
```

影响：

分类或标签关联可能写入 `undefined`，导致保存校验失败或关联数据错误。

建议：

- 共用稳定的原始 DTO，再分别通过 `select` 转换。
- 或拆分查询键，并统一分类/标签变更后的缓存失效。

### 8. 站内导航绕过未保存提醒

✅ **已修复（2026-09-12）**：迁移到数据路由（`createBrowserRouter`）+ `useBlocker`，覆盖 `<Link>`、命令式导航与浏览器后退；`beforeunload` 保留给刷新与关标签页。编辑器与通用表单引擎共用同一个守卫组件。

优先级：P1

验证状态：已动态复现

位置：

- `console/src/pages/content/editor.tsx:272-281`
- `console/src/components/form/schema-form.tsx:112-119`

问题：

当前仅监听 `beforeunload`，React Router 的站内导航不会触发该事件。

复现结果：

- 修改文章标题后不保存。
- 点击侧栏“文章”。
- 直接进入列表页，无确认对话框，修改丢失。

建议：

- 统一未保存状态保护。
- 覆盖 Link、命令式导航和浏览器后退。
- 保留 `beforeunload` 处理刷新和关闭页面。

### 9. Loading 状态不能可靠阻止重复提交

✅ **已修复（2026-09-12）**：按钮改为 `disabled = loading || disabled`；写操作再加一道同步 ref 闸门（`mutate` 在 pending 期间重复调用会重复发请求，而 state 更新是异步的）。

优先级：P1

验证状态：静态确认

位置：

- `console/src/components/ui/button.tsx:80`
- `console/src/pages/content/editor.tsx:637-643`

问题：

按钮实现为：

```tsx
disabled={asChild ? undefined : (disabled ?? loading)}
```

调用方传入 `disabled={false}` 时，`loading=true` 不会禁用按钮。

影响：

慢网环境连续点击保存可能产生重复 POST、重复创建文章或并发写入。

建议：

- 原生按钮使用 `disabled || loading`。
- 重要写操作增加独立的提交中保护。

### 10. 文章下线或转私密后评论仍公开读写

✅ **已修复（2026-09-12）**：Public 评论读写复用前台可见性判据（已发布且公开），不可见一律 404；Console 对全部状态评论的管理能力不变。

优先级：P1

验证状态：已动态复现

位置：

- `internal/comment/store.go:140-143`
- `internal/comment/handler.go:320-331`
- `internal/comment/handler.go:341-344`

问题：

Public 评论接口只验证文章引用是否存在，没有复用文章的发布状态与可见性规则。

复现结果：

1. 发布文章并创建已通过评论。
2. 撤回文章。
3. 匿名访问文章正文返回 404。
4. 匿名访问评论列表仍返回 200，并返回评论内容。
5. 匿名发表新评论仍被接受并写入数据库。

影响：

已下线或转私密内容仍可能通过评论接口泄露信息，并继续接收公开写入。

建议：

- Public 评论读写复用内容状态与可见性校验。
- 保留 Console 对全部状态评论的管理能力。

### 11. 永久删除与恢复并发可能删除已恢复内容

✅ **已修复（2026-09-12）**：`status = 'trashed'` 进入原子 DELETE 条件，条件不满足返回 409（`ErrNotTrashed`），不再依赖先读后删。

优先级：P1

验证状态：静态确认

位置：

- `internal/content/handler.go:438-447`
- `internal/content/store.go:201-207`

问题：

永久删除流程先读取并检查文章是否位于回收站，再单独执行 DELETE。检查和删除之间没有事务锁或条件约束。

触发条件：

1. 永久删除请求通过回收站检查后暂停。
2. 另一请求恢复该内容。
3. 原 DELETE 请求继续执行。

影响：

已恢复内容及其修订、评论等关联数据可能被不可逆级联删除。

建议：

- 将 `status = 'trashed'` 放入原子 DELETE 条件。
- 或在同一事务中锁定、检查并删除。

### 12. 正式 Release 不受测试门禁约束

✅ **已修复（2026-09-12）**：作业定义抽到可复用的 `.github/workflows/verify.yml`，CI 与 Release 调用同一套（lint / Console / Go 测试含 `-race` / 构建 / 交叉编译），Release 作业 `needs: verify`。

优先级：P1

验证状态：静态确认

位置：

- `.github/workflows/ci.yml:3-6`
- `.github/workflows/release.yml:24-27、62-74`
- `.goreleaser.yaml:95-99`

问题：

CI 仅支持手动触发，Release 不依赖测试工作流，也不检查同一提交是否通过验证。只要提交能够构建，推送版本标签就可能发布镜像并更新 `latest`。

建议：

- 将验证工作流改为可复用的 `workflow_call`。
- Release 强制等待同一提交的 Go 测试、PostgreSQL 集成测试、race 检测和前端检查。
- 不允许测试未通过的提交进入正式发布路径。

## 五、稳定版前建议完善的问题

### 1. 单连接池配置导致迁移永久等待

✅ **已修复（2026-09-12）**：迁移 advisory lock 改走容量 1 的独立连接池（goose 只接受 `*sql.DB`，无法共用会话），并新增可配置的锁等待超时（`LUMO_DATABASE_MIGRATION_LOCK_TIMEOUT`，默认 2 分钟），超时给出明确错误而不是永久挂起。

优先级：P2

验证状态：已动态复现

位置：

- `internal/config/config.go:292-300`
- `internal/migrate/migrate.go:232-234、261-267`
- `cmd/lumo/migrate.go:61-62`

问题：

迁移先占用一个连接持有 advisory lock，随后 Goose 从同一连接池再申请连接。`maxOpenConns=1` 时形成永久等待。

复现结果：

- `maxOpenConns=1`：超过 5 秒仍未完成。
- `maxOpenConns=2`：约 0.37 秒完成。

建议：

- 让锁与迁移工作共享受控连接。
- 或使用独立锁连接池。
- 最低限度应拒绝单连接配置并提供明确错误。
- 为迁移等待设置可配置期限。

### 2. 合法三级菜单必定被拒绝

✅ **已修复（2026-09-12）**：递归先判空数组再判层级。

优先级：P2

验证状态：已动态复现

位置：

- `internal/menu/handler.go:362-365`
- `internal/menu/handler.go:378-379`

问题：

递归在检查空 children 之前继续进入下一层。恰好三级、第三层没有 children 的合法菜单会被判断为第四层并返回 400。

复现结果：

- 二级菜单保存返回 200。
- 三级菜单保存返回 400，错误为“菜单层级最多 3 级”。

建议：

- 空数组提前返回。
- 或仅在存在子节点时递归。

### 3. 评论最大长度设置不生效

✅ **已修复（2026-09-12）**：按有效 `cfg.MaxLength` 校验 Unicode 字符数（设置值越界时回退到 10000 的硬上限）。

优先级：P2

验证状态：已动态复现

位置：

- `internal/comment/handler.go:351`
- `internal/comment/handler.go:511-518`
- `internal/comment/settings.go:44-45`

问题：

服务端只执行固定的 10000 字符硬上限，没有使用站点配置的有效 `maxLength`。

复现结果：

- 将 `maxLength` 设置为 100。
- 提交 101 个普通字符。
- 接口返回 201，评论成功入库。

建议：

- 提交时按有效 `cfg.MaxLength` 校验 Unicode 字符数。

### 4. Public 评论写入缺少 CSRF 校验

✅ **已修复（2026-09-12）**：Public 平面挂上 CSRF 中间件（匿名请求不带 Cookie，直接放行），主题的评论脚本带上 `X-CSRF-Token`。

优先级：P2

验证状态：静态确认，调用链明确

位置：

- `internal/api/api.go:90-102`
- `internal/comment/handler.go:126-135`
- `internal/comment/handler.go:419-425`

问题：

Public 平面会解析会话身份，但 CSRF 只挂载到 Console 和 Extension 平面。

触发条件：

- 攻击者控制与 CMS 同站点、不同源的页面，例如 HTTPS 下的兄弟子域。
- 受害者的 CMS 会话已登录。
- 目标文章允许评论。

影响：

攻击页面可借受害者会话写入评论；若受害者是文章作者，评论还可能自动通过审核。

建议：

- 对 Public 平面中使用会话身份的非安全方法执行 CSRF 校验。
- 同步更新主题评论客户端，使其携带 CSRF 头。

### 5. 并发保存菜单整树可能合并两份结果

✅ **已修复（2026-09-12）**：事务开头对 `menus` 父行 `SELECT ... FOR UPDATE`，同一菜单的整树替换串行化。回归测试已验证「去掉锁即失败」。

优先级：P2

验证状态：静态确认

位置：

- `internal/menu/store.go:141-161`

问题：

两个事务可以同时完成 DELETE，再分别 INSERT 自己的条目，最终结果可能包含 A 和 B 两棵树。

影响：

菜单页显示重复或混合导航，且两次请求都返回成功。

建议：

- 重建前锁定对应 `menus` 父行。
- 串行化同一菜单的整树替换。

### 6. ZIP 目录条目绕过数量限制

✅ **已修复（2026-09-12）**：新增 `MaxEntries`（主题与插件各 2500），文件、显式目录条目与文件路径隐含创建的父目录一并计入预算（目录去重）。

优先级：P2

验证状态：静态确认

位置：

- `internal/pkgzip/pkgzip.go:112-129`
- `internal/theme/package.go:102-107`
- `internal/plugin/package.go:95-100`

问题：

目录分支直接执行 `MkdirAll` 并 `continue`，不增加文件计数，也不计入总解压字节。大量目录条目可绕过当前限制。

影响：

安装第三方主题或插件时可能耗尽 inode/磁盘元数据，并造成长时间文件系统操作。

建议：

- 在落盘前限制归档总条目数。
- 把文件、显式目录和隐式创建的父目录纳入预算。
- 保留现有路径检查、符号链接拒绝和单文件字节限制。

### 7. 停机时邮件队列可能永久丢失

✅ **已修复（2026-09-12）**：停机顺序改为「HTTP 排空 → 拒绝入队 → 限时排空 → 停消费者」，未投递数量写入日志（>0 为 Warn）。

优先级：P2

验证状态：静态确认

位置：

- `internal/mail/module.go:62-65`
- `internal/mail/service.go:109-119`
- `cmd/lumo/serve.go:183-185`

问题：

邮件消费者直接使用信号 context，SIGTERM 到达后立即退出；HTTP 服务此时才开始排空，在途请求仍可向无消费者的内存 channel 写邮件。

影响：

停机时尚未发送的邮件可能丢失，重启后无法恢复。

建议：

- 先停止接收新请求并排空 HTTP。
- 再停止入队，并限时处理剩余邮件。
- 最后关闭消费者。
- 至少记录未完成邮件数量。

### 8. `/readyz` 缺少数据库探测期限

✅ **已修复（2026-09-12）**：readiness 探测使用独立的 3 秒期限，超时按数据库不可用返回 503；`/healthz` 不触库。

优先级：P2

验证状态：静态确认

位置：

- `cmd/lumo/serve.go:203-209`
- `internal/server/server.go:23-28`

问题：

`PingContext` 直接使用没有应用级期限的请求 context。HTTP `WriteTimeout` 不能终止等待连接池或数据库响应的处理器。

影响：

数据库连接黑洞或连接池耗尽时，readiness 可能长期不返回 503，并延长停机排空。

建议：

- 为 readiness 数据库探测设置 2 至 3 秒独立超时。
- 超时后明确返回 503。

### 9. Compose 可能在优雅关闭完成前强杀应用

✅ **已修复（2026-09-12）**：应用容器显式设置 `stop_grace_period: 40s`（HTTP 排空 15s + 模块清理 15s + 10s 余量，算式写在注释里）。

优先级：P2

验证状态：静态确认

位置：

- `deploy/docker-compose.yml:42-51`
- `internal/config/config.go:83-86`
- `internal/server/server.go:60-64`

问题：

Compose 默认停止宽限期为 10 秒，应用允许 HTTP 请求最多使用 15 秒收尾，之后还可能执行模块清理。

影响：

在途上传、请求或清理工作可能在正常部署重启时被 SIGKILL 截断。

建议：

- 显式设置应用容器的 `stop_grace_period`。
- 宽限期应大于 HTTP 排空与后续清理总预算。

### 10. 手动正式发布缺少 GHCR 登录

✅ **已修复（2026-09-12）**：登录条件与「是否真的发布」一致（`push` 标签，或手动且 `snapshot=false`）。

优先级：P2

验证状态：静态确认

位置：

- `.github/workflows/release.yml:53-60`
- `.github/workflows/release.yml:70-74`

问题：

登录 GHCR 的步骤只允许 `github.event_name == 'push'`。手动运行并设置 `snapshot=false` 时，GoReleaser 会进入真实发布模式，但没有镜像仓库凭据。

建议：

- 登录条件与真实发布条件保持一致。
- 或明确限制手动流程只能执行 snapshot。

### 11. 数据库特殊字符密码说明会造成首次部署失败

✅ **已修复（2026-09-12）**：口令拆成两个变量——`POSTGRES_PASSWORD` 保持原始口令交给 PostgreSQL 初始化，`LUMO_DATABASE_DSN` 单独填只对口令段编码的完整 URL；不填时按组件拼默认值以保住快速开始路径。`.env.example` 附对照示例与两种数据卷场景。

优先级：P2

验证状态：静态确认

位置：

- `deploy/.env.example:10-18`
- `deploy/docker-compose.yml:55`

问题：

`POSTGRES_PASSWORD` 同时被：

- 原样交给 PostgreSQL 初始化。
- 直接拼入应用使用的 URL DSN。

若按说明先做百分号编码，PostgreSQL 保存的是编码后的字符串，而客户端解析 DSN 后使用的却是解码后的值。

建议：

- 分开保存数据库原始密码和完整、正确编码的应用 DSN。
- 补充首次初始化和已有数据卷两种场景说明。

### 12. 备份恢复说明不足

✅ **已修复（2026-09-12）**：README 新增「备份与恢复」小节——pg_dump/pg_restore、应用卷打包与恢复（含 nonroot 属主）、S3 对象备份、密钥与环境变量备份、固定版本升级与两级回滚、恢复后检查清单。

优先级：P2

位置：

- `README.md:144`
- `deploy/docker-compose.yml:29-30`
- `deploy/docker-compose.yml:77-79`

问题：

数据库实际位于 `pgdata`，应用文件位于 `lumodata`。创建 `backups` 目录不代表已经实现备份。

需要补充：

- PostgreSQL 逻辑备份与恢复。
- 上传文件、主题、插件等应用卷备份。
- S3 对象备份。
- 环境变量与密钥备份。
- 固定版本升级、失败回滚和恢复后验证流程。

### 13. 构建不会自动清理旧 Console 资源

✅ **已修复（2026-09-12）**：新增 `task console:clean`（保留 `.gitkeep`），`console:build` 构建前先调用；`clean` 任务复用同一段逻辑。

优先级：P3

验证状态：静态确认

位置：

- `console/vite.config.ts:17-21`
- `Taskfile.yml:144-149`

问题：

`emptyOutDir: false` 保留旧 assets，发布构建没有统一清理步骤。长期构建可能让旧文件继续进入发布产物。

建议：

- 在统一构建任务中先清理旧资源。
- 单独保留 `.gitkeep`，确保 `go:embed all:dist` 可编译。

## 六、主要测试缺口

### 前端

当前仅有 4 个测试文件、59 个测试，主要覆盖认证入口、Schema 规则和表单渲染。缺少：

- 编辑器在不同文章 ID 之间切换。
- 保存失败后发布流程。
- 富文本与 Markdown 切换。
- 分类/标签缓存复用。
- 未保存内容的站内导航保护。
- 慢网重复提交。
- 长表单、编辑器和错误提示的移动端验证。

建议引入 Playwright 或等价的浏览器端到端测试，并覆盖上述场景。

### 后端

现有测试虽然覆盖全部数据库集成路径，但缺少以下并发和边界场景：

- PAT 派生权限不能超过父 PAT。
- 文章下线/私密后评论不可匿名访问和写入。
- 永久删除与恢复交错。
- 菜单整树并发替换。
- 三级菜单边界。
- 评论有效长度限制。
- `maxOpenConns=1` 的迁移行为。
- Public 会话写请求的 CSRF。
- ZIP 目录条目数量限制。

### 发布与运维

- 正式 Release 工作流必须依赖同一提交的完整测试。
- 本机和服务端都应执行 `go test -race`。
- 增加容器停机宽限期验证。
- 增加备份恢复演练。
- 增加特殊字符数据库密码的首次部署测试。
- 增加邮件排空或未完成任务记录测试。

## 七、建议处理顺序

> **2026-09-12 更新**：下面三个阶段列出的条目已全部完成（详见文首「修复状态」总表）。
> 第三阶段的「浏览器端到端测试」改为组件级回归测试（编辑器的五类问题各一条）；
> 「CI 恢复自动执行」未改回，保持手动触发，`verify.yml` 已可复用、加回 `push` / `pull_request` 即可自动跑。


第一阶段，发布前必须完成：

1. 修复受限 PAT 提权。
2. 明确正文信任模型并修复同源脚本执行风险。
3. 增加登录限流和 Argon2 并发预算。
4. 修复编辑器路由状态隔离、保存后发布和正文切换数据丢失。
5. 修复分类/标签缓存冲突和未保存导航保护。
6. 修复文章下线后评论仍公开读写。
7. 修复永久删除与恢复竞态。
8. 让正式 Release 强制依赖完整测试。

第二阶段，稳定版前完成：

1. 修复单连接池迁移死锁。
2. 修复三级菜单与评论长度设置。
3. 补齐 Public 评论 CSRF。
4. 修复菜单并发替换、ZIP 目录数量绕过和邮件停机丢失。
5. 补齐 readiness 超时、Compose 停机宽限期和手动 Release 登录。
6. 修正数据库密码编码说明和备份恢复文档。

第三阶段，持续质量控制：

1. 增加浏览器端到端测试和并发回归测试。
2. 将 CI 恢复为所有 PR/主分支提交自动执行。
3. 增加 race、容器、发布和恢复演练。
4. 将旧 Console 资源清理纳入统一构建流程。

## 八、最终判断

> **2026-09-12 更新**：文首「修复状态」总表列出的修复已完成，判定条件里的前四项（P1 权限与数据一致性、
> 编辑器数据丢失的回归测试、评论可见性与 CSRF 边界、Release 强制测试门禁）均已满足。
> **第五项「至少完成一次备份恢复和容器停机演练」仍未做**——本机无 Docker，
> 与「真实 Release 流程」「SMTP 停机排空实验」一并留待有 Docker / Actions 的环境执行。
> 因此本报告的建议仍然成立：在演练完成之前不要更新正式镜像的 `latest`。


当前项目已经具备较完整的业务骨架和较好的自动化测试基础，但还不能视为可直接正式发布的状态。

在下列条件满足前，不应更新正式镜像的 `latest`：

- P1 权限与数据一致性问题已修复。
- 编辑器数据丢失问题已有端到端回归测试。
- 评论可见性与 CSRF 边界已修复。
- Release 已强制经过完整测试门禁。
- 至少完成一次备份恢复和容器停机演练。

建议将本报告与对应修复提交关联，并在每项修复后补充可重复执行的回归测试，而不是只在代码中增加分支判断。
